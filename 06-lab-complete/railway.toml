[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'gunicorn --bind 0.0.0.0:${PORT:-8000} app:app --workers 2 --threads 4 --timeout 120'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
